apiVersion: v1
kind: Service
metadata:
  name: iis-default-svc
  labels:
    app: iis-default
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: iis-default